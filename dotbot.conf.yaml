- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.vim: vim
    ~/gitstat.sh: gitstat.sh
    ~/vimwiki.sh: vimwiki.sh
    ~/batstat.sh: batstat.sh
    ~/.config: config
    ~/.tmux.conf: tmux.conf
